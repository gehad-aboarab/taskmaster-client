<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.gehad.taskmaster">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".TaskApplication"
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.LoginActivity"
            android:label="Login" />
        <activity
            android:name=".activities.RegisterActivity"
            android:label="Register" />
        <activity
            android:name=".fragments.HomeActivity"
            android:label="TaskMaster" />
        <activity android:name=".fragments.ProjectSectionsActivity" />
        <activity
            android:name=".activities.ProjectInfoActivity"
            android:label="Project Info" />
        <activity
            android:name=".activities.SectionInfoActivity"
            android:label="Section Info" />
        <activity
            android:name=".activities.TaskInfoActivity"
            android:label="Task Info" />
        <activity
            android:name=".activities.AddProjectActivity"
            android:label="New Project" />
        <activity android:name=".activities.AddMemberActivity"></activity>
    </application>

</manifest>